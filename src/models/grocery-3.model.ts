export interface OfferCard {
  title: string;
  imgUrl: string;
  buttonText: string;
  discountOffer: string;
}

export interface MainCarouselItem {
  data?:any;
  title?: string;
  imgUrl?: string;
  buttonLink?: string;
  buttonText?: string;
  description?: string;
}
